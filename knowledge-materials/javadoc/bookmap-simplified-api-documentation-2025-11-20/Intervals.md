---
source_file: Intervals.html
package: velox.api.layer1.simplified
classes: Intervals
methods: INTERVAL50MILLISECONDS, INTERVAL100MILLISECONDS, INTERVAL200MILLISECONDS, INTERVAL250MILLISECONDS, INTERVAL500MILLISECONDS, INTERVAL750MILLISECONDS, INTERVAL1SECOND, INTERVAL2SECONDS, INTERVAL5SECONDS, INTERVAL10SECONDS, INTERVAL15SECONDS, INTERVAL20SECONDS, INTERVAL30SECONDS, INTERVAL1MINUTE, INTERVAL2MINUTES, INTERVAL5MINUTES, INTERVAL10MINUTES, INTERVAL15MINUTES, INTERVAL20MINUTES, INTERVAL30MINUTES
total_methods: 22
---

# Intervals

**Package:** velox.api.layer1.simplified

**Type:** Class

**Inheritance:** java.lang.Object → velox.api.layer1.simplified.Intervals

## Description

Just a bunch of common intervals. You might prefer to use `TimeUnit` directly instead.

## Fields

### INTERVAL_50_MILLISECONDS

```java
public static final long INTERVAL_50_MILLISECONDS
```

### INTERVAL_100_MILLISECONDS

```java
public static final long INTERVAL_100_MILLISECONDS
```

### INTERVAL_200_MILLISECONDS

```java
public static final long INTERVAL_200_MILLISECONDS
```

### INTERVAL_250_MILLISECONDS

```java
public static final long INTERVAL_250_MILLISECONDS
```

### INTERVAL_500_MILLISECONDS

```java
public static final long INTERVAL_500_MILLISECONDS
```

### INTERVAL_750_MILLISECONDS

```java
public static final long INTERVAL_750_MILLISECONDS
```

### INTERVAL_1_SECOND

```java
public static final long INTERVAL_1_SECOND
```

### INTERVAL_2_SECONDS

```java
public static final long INTERVAL_2_SECONDS
```

### INTERVAL_5_SECONDS

```java
public static final long INTERVAL_5_SECONDS
```

### INTERVAL_10_SECONDS

```java
public static final long INTERVAL_10_SECONDS
```

### INTERVAL_15_SECONDS

```java
public static final long INTERVAL_15_SECONDS
```

### INTERVAL_20_SECONDS

```java
public static final long INTERVAL_20_SECONDS
```

### INTERVAL_30_SECONDS

```java
public static final long INTERVAL_30_SECONDS
```

### INTERVAL_1_MINUTE

```java
public static final long INTERVAL_1_MINUTE
```

### INTERVAL_2_MINUTES

```java
public static final long INTERVAL_2_MINUTES
```

### INTERVAL_5_MINUTES

```java
public static final long INTERVAL_5_MINUTES
```

### INTERVAL_10_MINUTES

```java
public static final long INTERVAL_10_MINUTES
```

### INTERVAL_15_MINUTES

```java
public static final long INTERVAL_15_MINUTES
```

### INTERVAL_20_MINUTES

```java
public static final long INTERVAL_20_MINUTES
```

### INTERVAL_30_MINUTES

```java
public static final long INTERVAL_30_MINUTES
```

### MIN_INTERVAL

```java
public static final long MIN_INTERVAL
```

Smallest interval allowed

## Constructors

### Intervals

```java
public Intervals()
```